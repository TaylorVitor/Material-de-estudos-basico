
/*

O que são metodos? Métodos são propriedades que funcionam como funções

strings arrays contém métodos

*/

list = ["Taylor", "Matheus", "Marcelo", "Vagen"]

let newlist = list.map(nomes => {
    console.log(nomes.toUpperCase())
})